# Copiar para .env NO SERVIDOR (nao versionado).
# JWT_SECRET deve ser o MESMO valor que esta hoje no dashboard do Render,
# senao os tokens salvos no navegador de voces expiram e ambos precisam relogar.
JWT_SECRET=
